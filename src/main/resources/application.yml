spring:
  liquibase:
    change-log: classpath:/migration/master-changelog.xml
  datasource:
    url: jdbc:postgresql://localhost:5432/order
    username: postgres
    password: linh2002
  application:
    name: order-service
